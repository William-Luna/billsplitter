import { createTheme } from "@mui/material"

export const appTheme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1565C0',
    },
    secondary: {
      main: '#f50057',
    },
    background: {
      default: '#21374e',
    }
  }
})